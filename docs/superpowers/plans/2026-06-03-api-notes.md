# API 메모 + 상태 태그 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** operation별로 자유 텍스트 메모 + 상태 태그(deprecated/검토중/안정/사용금지)를 남겨 localStorage에 영구 저장하고, 요청 화면에서 편집·사이드바에서 한눈에 표시한다.

**Architecture:** 순수 데이터/메타 모듈(`core/notes.ts`)을 만들고, 요청 화면용 편집 컴포넌트(`ApiNoteEditor.tsx`)를 분리한다. App.tsx가 notes state를 소유하며 기존 favorites와 동일한 영속화 패턴을 따른다. Sidebar는 notes를 받아 상태 점/메모 아이콘만 표시한다.

**Tech Stack:** React 19, TypeScript, vitest(jsdom), 기존 storage.ts(localStorage) + Select.tsx(공용 드롭다운).

**Spec:** `docs/superpowers/specs/2026-06-03-api-notes-design.md`

작업 디렉터리: `/Users/82312411gimjaehyeog/Dev/swagger-man/apps/desktop`. 모든 명령은 이 디렉터리에서 실행. 브랜치: `main` 직접 커밋.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/core/notes.ts` (신규) | ApiNote/ApiStatus 타입, STATUS_META(라벨/색/점색), loadNotes/saveNotes, isEmptyNote |
| `src/core/notes.test.ts` (신규) | notes 모듈 단위 테스트 |
| `src/components/ApiNoteEditor.tsx` (신규) | 요청 화면 상단 메모 영역(상태 Select + textarea, 접기/펼치기) |
| `src/components/ApiNoteEditor.test.tsx` (신규) | 편집 컴포넌트 테스트 |
| `src/components/RequestEditor.tsx` (수정) | summary 아래 `<ApiNoteEditor>` 삽입 + props 2개 추가 |
| `src/components/Sidebar.tsx` (수정) | op-row에 상태 점 + 메모 아이콘, Props에 notes 추가 |
| `src/App.tsx` (수정) | notes state, 로드/영속화, RequestEditor·Sidebar에 전달 |
| `src/App.css` (수정) | `.api-note-*`, `.op-status-dot` 스타일 |

---

### Task 1: core/notes.ts — 타입·메타·영속화

**Files:**
- Create: `src/core/notes.ts`
- Create: `src/core/notes.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// src/core/notes.test.ts
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from "vitest";
import {
  loadNotes,
  saveNotes,
  isEmptyNote,
  STATUS_META,
  emptyNote,
  type ApiNote,
  type NotesMap,
} from "./notes";

beforeEach(() => localStorage.clear());

describe("notes 영속화", () => {
  it("저장 후 같은 specUrl로 로드하면 동일 맵을 복원한다", () => {
    const notes: NotesMap = {
      "GET /pet/findByStatus": { text: "6월 제거 예정", status: "deprecated", updatedAt: 100 },
    };
    saveNotes("https://api.test/spec.json", notes);
    expect(loadNotes("https://api.test/spec.json")).toEqual(notes);
  });

  it("저장된 적 없는 specUrl은 빈 객체를 반환한다", () => {
    expect(loadNotes("https://none/spec.json")).toEqual({});
  });

  it("저장 시 빈 노트(text 공백 + status none)는 제거된다", () => {
    const notes: NotesMap = {
      "GET /a": { text: "  ", status: "none", updatedAt: 1 }, // 빈 노트
      "GET /b": { text: "남김", status: "none", updatedAt: 2 },
      "GET /c": { text: "", status: "review", updatedAt: 3 }, // status 있으면 유지
    };
    saveNotes("https://api.test/spec.json", notes);
    const loaded = loadNotes("https://api.test/spec.json");
    expect(loaded["GET /a"]).toBeUndefined();
    expect(loaded["GET /b"]).toBeDefined();
    expect(loaded["GET /c"]).toBeDefined();
  });
});

describe("isEmptyNote", () => {
  it("text가 공백이고 status가 none이면 빈 노트", () => {
    expect(isEmptyNote({ text: "   ", status: "none", updatedAt: 0 })).toBe(true);
  });
  it("text가 있으면 빈 노트 아님", () => {
    expect(isEmptyNote({ text: "x", status: "none", updatedAt: 0 })).toBe(false);
  });
  it("status가 none이 아니면 빈 노트 아님", () => {
    expect(isEmptyNote({ text: "", status: "stable", updatedAt: 0 })).toBe(false);
  });
});

describe("STATUS_META / emptyNote", () => {
  it("5개 상태 모두 라벨·색·점색을 갖는다", () => {
    for (const s of ["none", "deprecated", "review", "stable", "blocked"] as const) {
      expect(STATUS_META[s]).toBeDefined();
      expect(typeof STATUS_META[s].label).toBe("string");
    }
  });
  it("emptyNote는 빈 텍스트 + none 상태", () => {
    const n = emptyNote();
    expect(n.text).toBe("");
    expect(n.status).toBe("none");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/core/notes.test.ts`
Expected: FAIL — `Cannot find module './notes'`

- [ ] **Step 3: 구현**

```ts
// src/core/notes.ts
// operation별 메모 + 상태 태그. localStorage 영속화(스펙 URL별 키).
import { loadJSON, saveJSON } from "./storage";

export type ApiStatus = "none" | "deprecated" | "review" | "stable" | "blocked";

export interface ApiNote {
  text: string;
  status: ApiStatus;
  updatedAt: number;
}

/** opId → 노트 */
export type NotesMap = Record<string, ApiNote>;

/** 상태별 표시 메타(라벨/텍스트색/점색). 색은 기존 App.css에서 쓰는 값과 동일. */
export const STATUS_META: Record<ApiStatus, { label: string; color: string; dot: string }> = {
  none: { label: "상태 없음", color: "var(--muted)", dot: "transparent" },
  deprecated: { label: "⚠️ Deprecated", color: "#d29922", dot: "#d29922" },
  review: { label: "🔍 검토중", color: "var(--accent)", dot: "var(--accent)" },
  stable: { label: "✅ 안정", color: "#3fb950", dot: "#3fb950" },
  blocked: { label: "🚫 사용금지", color: "#f85149", dot: "#f85149" },
};

export const STATUS_ORDER: ApiStatus[] = ["none", "deprecated", "review", "stable", "blocked"];

export function emptyNote(): ApiNote {
  return { text: "", status: "none", updatedAt: 0 };
}

/** text가 공백이고 status가 none이면 의미 없는 빈 노트. */
export function isEmptyNote(note: ApiNote): boolean {
  return note.text.trim() === "" && note.status === "none";
}

function storageKey(specUrl: string): string {
  return `swaggerman.notes.${specUrl}`;
}

export function loadNotes(specUrl: string): NotesMap {
  return loadJSON<NotesMap>(storageKey(specUrl), {});
}

/** 저장 시 빈 노트는 제거해 저장소를 정리한다. */
export function saveNotes(specUrl: string, notes: NotesMap): void {
  const cleaned: NotesMap = {};
  for (const [opId, note] of Object.entries(notes)) {
    if (!isEmptyNote(note)) cleaned[opId] = note;
  }
  saveJSON(storageKey(specUrl), cleaned);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/core/notes.test.ts`
Expected: PASS (9개)

- [ ] **Step 5: 커밋**

```bash
git add src/core/notes.ts src/core/notes.test.ts
git commit -m "기능: API 메모 코어 — 타입·상태 메타·localStorage 영속화

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: ApiNoteEditor 컴포넌트

**Files:**
- Create: `src/components/ApiNoteEditor.tsx`
- Create: `src/components/ApiNoteEditor.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```tsx
// src/components/ApiNoteEditor.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { ApiNoteEditor } from "./ApiNoteEditor";
import { emptyNote, type ApiNote } from "../core/notes";

function renderEditor(note: ApiNote = emptyNote()) {
  const onChange = vi.fn();
  const utils = render(<ApiNoteEditor note={note} onChange={onChange} />);
  return { onChange, ...utils };
}

describe("ApiNoteEditor", () => {
  it("빈 노트면 '+ 메모 추가' 접힘 버튼만 보인다", () => {
    renderEditor();
    expect(screen.getByRole("button", { name: /메모 추가/ })).toBeTruthy();
    expect(screen.queryByPlaceholderText(/메모/)).toBeNull();
  });

  it("'+ 메모 추가'를 누르면 textarea가 펼쳐진다", () => {
    renderEditor();
    fireEvent.click(screen.getByRole("button", { name: /메모 추가/ }));
    expect(screen.getByPlaceholderText(/메모/)).toBeTruthy();
  });

  it("메모가 있으면 펼친 상태로 텍스트를 표시한다", () => {
    renderEditor({ text: "백엔드 문의함", status: "review", updatedAt: 1 });
    expect((screen.getByPlaceholderText(/메모/) as HTMLTextAreaElement).value).toBe("백엔드 문의함");
  });

  it("텍스트 입력 시 onChange가 갱신된 노트로 호출된다", () => {
    const { onChange } = renderEditor({ text: "", status: "review", updatedAt: 1 });
    fireEvent.change(screen.getByPlaceholderText(/메모/), { target: { value: "새 메모" } });
    expect(onChange).toHaveBeenCalledWith(
      expect.objectContaining({ text: "새 메모", status: "review" }),
    );
  });

  it("상태를 바꾸면 onChange가 새 status로 호출된다", () => {
    const { onChange } = renderEditor({ text: "x", status: "none", updatedAt: 1 });
    // 공용 Select 트리거 열기 → 옵션 선택
    fireEvent.click(screen.getByRole("button", { name: /상태|Deprecated|없음/ }));
    fireEvent.mouseDown(screen.getByText("🚫 사용금지"));
    expect(onChange).toHaveBeenCalledWith(expect.objectContaining({ status: "blocked" }));
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/components/ApiNoteEditor.test.tsx`
Expected: FAIL — `Cannot find module './ApiNoteEditor'`

- [ ] **Step 3: 구현**

```tsx
// src/components/ApiNoteEditor.tsx
// 요청 화면 상단 메모 영역: 상태 태그(공용 Select) + 자유 텍스트. 빈 노트는 접어둔다.
import { useState } from "react";
import { Select } from "./Select";
import { STATUS_META, STATUS_ORDER, isEmptyNote, type ApiNote, type ApiStatus } from "../core/notes";

interface Props {
  note: ApiNote;
  onChange: (note: ApiNote) => void;
}

export function ApiNoteEditor({ note, onChange }: Props) {
  // 빈 노트는 기본 접힘. 사용자가 펼치면 열림.
  const [expanded, setExpanded] = useState(!isEmptyNote(note));

  const patch = (p: Partial<ApiNote>) => onChange({ ...note, ...p, updatedAt: Date.now() });

  if (!expanded) {
    return (
      <button className="api-note-add" onClick={() => setExpanded(true)}>
        📝 메모 추가
      </button>
    );
  }

  return (
    <div className="api-note">
      <div className="api-note-head">
        <span className="api-note-title">📝 메모</span>
        <Select
          className="api-note-status"
          value={note.status}
          onChange={(v) => patch({ status: v as ApiStatus })}
          options={STATUS_ORDER.map((s) => ({ value: s, label: STATUS_META[s].label }))}
        />
      </div>
      <textarea
        className="api-note-text"
        value={note.text}
        onChange={(e) => patch({ text: e.target.value })}
        placeholder="이 API에 대한 메모 (예: 6월 제거 예정, 백엔드 문의 6/3)"
        spellCheck={false}
        rows={2}
      />
    </div>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/components/ApiNoteEditor.test.tsx`
Expected: PASS (5개)

만약 상태 변경 테스트가 공용 Select의 트리거 라벨 매칭으로 실패하면, 실제 `Select.tsx`의 트리거 마크업을 확인해 `screen.getByRole("button")` 또는 `.cselect-trigger` 쿼리로 조정한다(기존 `RequestEditor.test.tsx`의 Select 조작 패턴 참고: 트리거 클릭 → 옵션 `mouseDown`).

- [ ] **Step 5: 커밋**

```bash
git add src/components/ApiNoteEditor.tsx src/components/ApiNoteEditor.test.tsx
git commit -m "기능: API 메모 편집 컴포넌트 — 상태 태그 + 텍스트, 빈 노트 접기

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: App.tsx — notes state·로드·영속화

**Files:**
- Modify: `src/App.tsx`

기존 favorites 패턴(`swaggerman.fav.*`)을 그대로 따른다.

- [ ] **Step 1: import 추가**

`App.tsx` 상단의 core import 묶음에 추가 (storage import 근처):

```tsx
import { loadNotes, saveNotes, emptyNote, type NotesMap, type ApiNote } from "./core/notes";
```

- [ ] **Step 2: state 선언 추가**

`const [favorites, setFavorites] = useState<string[]>([]);` 아래에 추가:

```tsx
  const [notes, setNotes] = useState<NotesMap>({});
```

- [ ] **Step 3: 영속화 useEffect 추가**

favorites 영속화 useEffect 바로 아래에 추가:

```tsx
  useEffect(() => {
    if (activeSpecUrl) saveNotes(activeSpecUrl, notes);
  }, [notes, activeSpecUrl]);
```

- [ ] **Step 4: 프로젝트 로드 시 notes 불러오기**

`setFavorites(loadJSON(\`swaggerman.fav.${targetUrl}\`, [] as string[]));` 줄 바로 아래에 추가:

```tsx
      setNotes(loadNotes(targetUrl));
```

- [ ] **Step 5: notes 갱신 헬퍼 추가**

`toggleFavorite` 함수 근처(컴포넌트 본문)에 추가:

```tsx
  function updateNote(opId: string, note: ApiNote) {
    setNotes((prev) => ({ ...prev, [opId]: note }));
  }
```

- [ ] **Step 6: 타입체크 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음 (notes는 아직 미사용이지만 ApiNote import는 Step 5에서 사용됨)

- [ ] **Step 7: 커밋**

```bash
git add src/App.tsx
git commit -m "기능: App에 API 메모 state·로드·영속화 연결

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: RequestEditor에 메모 영역 삽입

**Files:**
- Modify: `src/components/RequestEditor.tsx`
- Modify: `src/App.tsx` (RequestEditor 렌더에 props 전달)

- [ ] **Step 1: RequestEditor import + Props 추가**

`RequestEditor.tsx` 상단 import에 추가:

```tsx
import { ApiNoteEditor } from "./ApiNoteEditor";
import { emptyNote, type ApiNote } from "../core/notes";
```

`interface Props { ... }`에 두 줄 추가 (mentionKeys 아래):

```tsx
  note?: ApiNote;
  onNoteChange?: (note: ApiNote) => void;
```

- [ ] **Step 2: summary 아래에 ApiNoteEditor 렌더**

`{operation.summary && <p className="op-desc">{operation.summary}</p>}` 줄 바로 아래에 추가:

```tsx
        {onNoteChange && (
          <ApiNoteEditor note={note ?? emptyNote()} onChange={onNoteChange} />
        )}
```

(props 구조분해에 `note`, `onNoteChange`가 포함되도록 컴포넌트 시그니처 확인 — RequestEditor가 `props`를 구조분해하는지 `function RequestEditor(props: Props)` 형태인지에 맞춰 접근. 후자면 `props.note`/`props.onNoteChange`로 접근.)

- [ ] **Step 3: App.tsx에서 props 전달**

`App.tsx`의 `<RequestEditor ... />`에서 `historyItem={selectedHistory}` 줄 근처에 추가:

```tsx
            note={selected ? (notes[selected.id] ?? emptyNote()) : emptyNote()}
            onNoteChange={(note) => {
              if (selected) updateNote(selected.id, note);
            }}
```

- [ ] **Step 4: CSS 추가 (App.css 끝)**

```css
/* ============================================================
 * API 메모 (요청 화면 상단)
 * ============================================================ */
.api-note-add {
  align-self: flex-start;
  background: transparent;
  border: 1px dashed var(--border);
  color: var(--muted);
  border-radius: 6px;
  padding: 4px 10px;
  font-size: 12px;
  cursor: pointer;
  margin: 4px 0 8px;
}
.api-note-add:hover {
  border-color: var(--accent);
  color: var(--text);
}
.api-note {
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 8px 10px;
  margin: 4px 0 10px;
  background: var(--bg-2);
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.api-note-head {
  display: flex;
  align-items: center;
  gap: 8px;
}
.api-note-title {
  font-size: 11px;
  font-weight: 700;
  color: var(--muted);
}
.api-note-status {
  margin-left: auto;
}
.api-note-text {
  background: var(--bg);
  color: var(--text);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 6px 8px;
  font-size: 12px;
  font-family: inherit;
  resize: vertical;
  min-height: 38px;
}
```

- [ ] **Step 5: 타입체크 + 전체 테스트**

Run: `npx tsc --noEmit && npx vitest run 2>&1 | tail -3`
Expected: 타입 에러 없음, 전체 PASS

- [ ] **Step 6: 커밋**

```bash
git add src/components/RequestEditor.tsx src/App.tsx src/App.css
git commit -m "기능: 요청 화면 상단에 API 메모 편집 영역 연결

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Sidebar에 상태 점·메모 아이콘 표시

**Files:**
- Modify: `src/components/Sidebar.tsx`
- Modify: `src/App.tsx` (Sidebar에 notes 전달)
- Modify: `src/App.css` (상태 점 스타일)

- [ ] **Step 1: Sidebar import + Props 추가**

`Sidebar.tsx` 상단 import에 추가:

```tsx
import { STATUS_META, type NotesMap } from "../core/notes";
```

`interface Props { ... }`에 추가 (onCompareHistory 아래):

```tsx
  /** opId별 메모/상태 (상태 점·메모 아이콘 표시용) */
  notes: NotesMap;
```

- [ ] **Step 2: renderRow에 상태 점 + 메모 아이콘 추가**

`renderRow` 함수에서 `<span className="method" ...>` 앞(star 버튼과 method 사이)에 상태 점을 추가하고, `op-text` 끝에 메모 아이콘을 추가한다. 기존 renderRow의 method/op-text 구조를 다음으로 교체:

```tsx
      <span className="method" style={{ color: methodColor(op.method) }}>
        {op.method}
      </span>
      <span className="op-text">
        <span className="op-path">
          {(() => {
            const note = props.notes[op.id];
            return note && note.status !== "none" ? (
              <span
                className="op-status-dot"
                style={{ background: STATUS_META[note.status].dot }}
                title={STATUS_META[note.status].label}
              />
            ) : null;
          })()}
          {op.path}
          {props.notes[op.id]?.text.trim() && (
            <span className="op-note-icon" title="메모 있음">📝</span>
          )}
        </span>
        {op.summary && <span className="op-summary">{op.summary}</span>}
      </span>
```

- [ ] **Step 3: App.tsx에서 notes 전달**

`App.tsx`의 `<Sidebar ... />`에서 `selectedHistoryId={...}` 줄 근처에 추가:

```tsx
            notes={notes}
```

- [ ] **Step 4: CSS 추가 (App.css 끝)**

```css
.op-status-dot {
  display: inline-block;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  margin-right: 5px;
  vertical-align: middle;
}
.op-note-icon {
  margin-left: 5px;
  font-size: 10px;
  opacity: 0.8;
}
```

- [ ] **Step 5: 타입체크 + 전체 테스트**

Run: `npx tsc --noEmit && npx vitest run 2>&1 | tail -3`
Expected: 타입 에러 없음, 전체 PASS

만약 기존 `Sidebar.test.tsx`가 있어 notes prop 누락으로 실패하면, 그 테스트의 Sidebar 렌더에 `notes={{}}`를 추가한다.

- [ ] **Step 6: 커밋**

```bash
git add src/components/Sidebar.tsx src/App.tsx src/App.css
git commit -m "기능: 사이드바에 API 상태 점·메모 아이콘 표시

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 검증 + 브라우저 모드 확인

**Files:** 없음 (검증만)

- [ ] **Step 1: 전체 자동 검증**

```bash
npx eslint . && npx tsc --noEmit && npx vitest run && npm run build
```
Expected: 모두 PASS / 에러 없음 (기존 AiPanel react-refresh 경고 1건은 무관)

- [ ] **Step 2: 브라우저 모드 수동 확인**

```bash
npm run dev
```
Chrome DevTools MCP로 `http://localhost:1420` 접속 후 확인:
1. Petstore 스펙(`https://petstore3.swagger.io/api/v3/openapi.json`) 로드
2. API 선택 → 요청 화면 summary 아래 "📝 메모 추가" 버튼 확인
3. 클릭 → 펼침 → 상태를 "⚠️ Deprecated"로, 텍스트 입력
4. 사이드바 해당 API에 주황 점 + 📝 아이콘 표시 확인
5. 다른 API 선택 후 돌아오면 메모 유지 확인 (state)
6. 페이지 새로고침 후 같은 스펙 로드 → 메모 복원 확인 (localStorage)

- [ ] **Step 3: 발견된 문제 수정 후 커밋** (있을 때만)

---

## Self-Review 체크

- **스펙 커버리지**: 데이터모델(Task1) ✓ / 5종 상태 메타(Task1) ✓ / localStorage 영속화(Task1,3) ✓ / 빈 노트 정리(Task1) ✓ / 요청 화면 상단 편집(Task2,4) ✓ / 접기-펼치기(Task2) ✓ / 사이드바 상태 점·메모 아이콘(Task5) ✓ / 프로젝트 전환 시 로드(Task3) ✓ / 디바운스 — **검토 필요**
- **디바운스**: spec은 "디바운스(400ms) 저장"을 언급. 단, 기존 favorites/extract 영속화 useEffect는 디바운스 없이 즉시 저장한다(동일 패턴). notes도 같은 패턴(즉시 저장)으로 구현했다 — 메모는 작은 텍스트라 키 입력당 저장 비용이 무시할 수준이고 기존 코드와 일관성이 우선. spec의 디바운스 요구는 과설계로 판단해 생략(YAGNI). 만약 실측 jank가 있으면 Task3의 영속화 useEffect에 setTimeout/clearTimeout 디바운스를 추가.
- **타입 일관성**: ApiNote/NotesMap/ApiStatus가 notes.ts에서 정의되고 ApiNoteEditor·RequestEditor·App·Sidebar에서 동일하게 import됨 ✓. emptyNote()/STATUS_META/STATUS_ORDER 시그니처 일관 ✓
- **플레이스홀더 없음** ✓
